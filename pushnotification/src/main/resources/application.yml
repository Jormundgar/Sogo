eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8083

spring:
  application:
    name: push-notification

# Swagger Configuration
springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/swagger.html"